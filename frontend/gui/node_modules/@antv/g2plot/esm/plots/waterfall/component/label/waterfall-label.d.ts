import ColumnLabel, { IColumnLabel } from '../../../column/component/label';
export default class WaterfallLabels extends ColumnLabel {
    constructor(cfg: IColumnLabel);
    adjustLabel(label: any, shape: any): void;
}

import React, { HTMLAttributes } from 'react';
import ComboPlot, { ComboPlotConfig } from '@antv/g2plot/lib/combo-plots/base';
declare type PickedAttrs = 'className' | 'style';
interface Plot<C extends ComboPlotConfig = ComboPlotConfig> {
    new (container: HTMLElement, props: C): ComboPlot<C>;
}
export interface ComboChartProps<C extends ComboPlotConfig = ComboPlotConfig> extends Pick<HTMLAttributes<HTMLDivElement>, PickedAttrs> {
    chart: Plot<C>;
    onMount?: (chart: ComboPlot<C>) => void;
}
export default class ComboChart<C extends ComboPlotConfig = ComboPlotConfig> extends React.Component<ComboChartProps<C>> {
    private el;
    private chart?;
    private getContainer;
    private getConfig;
    componentDidMount(): void;
    componentDidUpdate(): void;
    componentWillUnmount(): void;
    render(): JSX.Element;
}
export {};

import React, { HTMLAttributes } from 'react';
import { Base as BasePlot, ViewLayer, PlotConfig } from '@antv/g2plot';
declare type PickedAttrs = 'className' | 'style';
export declare type LayerCtor<C> = ViewLayer<C>;
export interface Plot<C extends PlotConfig = PlotConfig> {
    new (container: HTMLElement, props: C): BasePlot<C, LayerCtor<C>>;
}
export interface BaseChartProps<C extends PlotConfig = PlotConfig> extends Pick<HTMLAttributes<HTMLDivElement>, PickedAttrs> {
    chart: Plot<C>;
    onMount?: (chart: BasePlot<C, LayerCtor<C>>) => void;
}
export default class BaseChart<C extends PlotConfig = PlotConfig> extends React.Component<BaseChartProps<C>> {
    private el;
    private chart?;
    private config?;
    private getContainer;
    private getConfig;
    componentDidMount(): void;
    componentDidUpdate(): void;
    componentWillUnmount(): void;
    render(): JSX.Element;
}
export {};
